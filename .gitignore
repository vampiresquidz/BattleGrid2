# dependencies & build output
node_modules/
dist/

# secrets — NEVER commit
.env
.env.*
*.key

# logs / OS cruft
*.log
.DS_Store
Thumbs.db

# local agent / editor state
.claude/

# QA artifacts generated by the tools (keep the scripts, drop their output)
tools/*.png
tools/*.jpg
tools/*.mp4
tools/*.gif
tools/*.webm
tools/_*/

# heavy dev-only source art (not needed to run/host; re-add via Git LFS if wanted)
assets/
_refs/
